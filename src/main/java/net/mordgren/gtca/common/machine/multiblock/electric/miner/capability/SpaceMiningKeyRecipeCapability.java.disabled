// package net.mordgren.gtca.common.machine.multiblock.electric.miner.capability;
//
// import com.gregtechceu.gtceu.api.capability.recipe.RecipeCapability;
// import com.gregtechceu.gtceu.api.recipe.chance.logic.ChanceLogic;
// import com.gregtechceu.gtceu.api.recipe.content.Content;
// import com.gregtechceu.gtceu.api.recipe.lookup.ingredient.AbstractMapIngredient;
// import com.gregtechceu.gtceu.api.recipe.lookup.ingredient.MapIngredientTypeManager;
//
// import java.lang.reflect.Field;
// import java.util.ArrayList;
// import java.util.List;
// import java.util.Map;
//
// public final class SpaceMiningKeyRecipeCapability extends RecipeCapability<SpaceMiningKey> {
//
//     public static final SpaceMiningKeyRecipeCapability CAP = new SpaceMiningKeyRecipeCapability();
//
//     private static boolean MAP_TYPE_REGISTERED = false;
//
//     private SpaceMiningKeyRecipeCapability() {
//         super(
//                 "gtca:space_mining_key",
//                 0xFF55FF55,
//                 true,   // ВАЖНО: должно быть TRUE, иначе ключ не участвует в поиске и рецепты “схлопываются”
//                 1000,
//                 SpaceMiningKeySerializer.INSTANCE
//         );
//     }
//
//     /** Вызвать ОДИН раз в common setup (enqueueWork). */
//     public static void registerMapIngredientType() {
//         if (MAP_TYPE_REGISTERED) return;
//         MAP_TYPE_REGISTERED = true;
//
//         // Регистрируем конвертацию SpaceMiningKey -> AbstractMapIngredient
//         MapIngredientTypeManager.registerMapIngredient(
//                 SpaceMiningKey.class,
//                 key -> List.of(new SpaceMiningKeyMapIngredient(key))
//         );
//     }
//
//     /** На всякий случай: дефолтная конвертация (если где-то не сработает менеджер). */
//     @Override
//     public List<AbstractMapIngredient> getDefaultMapIngredient(Object object) {
//         if (object instanceof SpaceMiningKey key) {
//             return List.of(new SpaceMiningKeyMapIngredient(key));
//         }
//         return null;
//     }
//
//     /** Кладём ключ в builder.inputs через рефлексию (как ты делал с Info). */
//     public static void putKey(Object recipeBuilder, SpaceMiningKey key) {
//         if (recipeBuilder == null || key == null) return;
//
//         try {
//             Field f = findField(recipeBuilder.getClass(), "input", "inputs");
//             if (f == null) return;
//             f.setAccessible(true);
//
//             Object mapObj = f.get(recipeBuilder);
//             if (!(mapObj instanceof Map<?, ?>)) return;
//
//             @SuppressWarnings("unchecked")
//             Map<RecipeCapability<?>, List<Content>> map =
//                     (Map<RecipeCapability<?>, List<Content>>) mapObj;
//
//             int max = ChanceLogic.getMaxChancedValue();
//             Content c = new Content(key, max, max, 0);
//
//             map.computeIfAbsent(CAP, k -> new ArrayList<>()).add(c);
//         } catch (Throwable ignored) {
//         }
//     }
//
//     private static Field findField(Class<?> cls, String... names) {
//         for (String n : names) {
//             try { return cls.getField(n); } catch (Throwable ignored) {}
//             try {
//                 Field f = cls.getDeclaredField(n);
//                 f.setAccessible(true);
//                 return f;
//             } catch (Throwable ignored) {}
//         }
//         return null;
//     }
// }